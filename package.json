{
  "name": "cursor-projects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env.local index.js",
    "serve": "node --env-file=.env.local index.js --serve",
    "migrate": "node --env-file=.env.local scripts/migrate.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.21.0"
  }
}
